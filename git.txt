Global setup:

 Download and install Git
  git config --global user.name "Blanca Vargas Govea"
  git config --global user.email blanca.vg@gmail.com
        

Next steps:

  mkdir taller-R
  cd taller-R
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:blancavg/taller-R.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:blancavg/taller-R.git
  git push -u origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue


